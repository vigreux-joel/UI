import { FunctionComponent } from "react";
import { ButtonProps } from "@/components/button/button";

export enum TabsVariant {
  Primary = "primary",
  Secondary = "secondary",
}

export const Tabs: FunctionComponent<ButtonProps> = ({}) => {
  return <p></p>;
};
